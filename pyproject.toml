[project]
name = "fedmed"
version = "0.1.0"
description = "Privacy-preserving federated MRI segmentation prototype"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26"]

[tool.unittest]
start-directory = "tests"
